import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
	container: {
		flexDirection: 'row'
	},
	itemTerm: {
		color: "#000",
		fontSize: 16,
		fontWeight: 'bold' 
	},
	itemValue: {
		color: "#000",
		fontSize: 16
	}
});

export { styles };
