import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
	container: {
		width: "100%",
		paddingHorizontal: 20,
		marginBottom: 15,
	},
	input: {
		backgroundColor: '#FFF',
		padding: 10,
		borderRadius: 8,
		fontSize: 17,
		color: '#121212',
	}
});

export { styles }
