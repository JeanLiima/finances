import { Text, View } from "react-native";

import { styles } from "./styles";

const Categories = () => {
	return (
		<View style={styles.container}>
			<Text>User configurations</Text>
		</View>
	);
};

export { Categories };
