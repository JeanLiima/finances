import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
	container: {
		flex: 1,
		backgroundColor: "#F0F4FF",
		padding: 16,
	},
	label: {
		fontSize: 16,
		color: "#333",
		fontWeight: "500",
		marginBottom: 15
	}
});
