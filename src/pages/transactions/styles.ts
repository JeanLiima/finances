import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
	container: { 
		flex:1,
  		backgroundColor: "#F0F4FF",
	},
	list: {
		flex: 1,
		marginTop: 8,
		marginLeft: 8,
		marginRight: 8,
	}
});

export { styles };
